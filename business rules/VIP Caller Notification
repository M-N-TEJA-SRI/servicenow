(function executeRule(current, previous /*null when async*/) {
var gr=new GlideRecord('sys_user');
if(gr.get(current.getValue('caller_id')))//point to the record in sys_user
{
	if(gr.getValue('vip'))//returns true if vip field is checked
	{
		current.work_notes='caller is a vip person';//work notes will be added
		current.update();//after br does not save automatically so we are saving it manually
	}
	else{
		gs.addInfoMessage('caller is not a vip person');//if the person is not a vip person then info message is printed
	}

}
else{
	gs.addErrorMessage('caller is not found');
}
	// Add your code here

})(current, previous);
